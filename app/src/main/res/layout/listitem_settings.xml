<?xml version="1.0" encoding="utf-8"?>
<LinearLayout android:id="@+id/settingsLayout"
              xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="fill_parent"
              android:layout_height="48dp"
              android:gravity="center"
              android:orientation="vertical">

    <RelativeLayout
        android:id="@+id/view_item"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@color/settings_list_item_bg_color_70">

        <ImageView
            android:id="@+id/image_res"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/ripple_bg"
            android:padding="@dimen/margin_gap_8"
            android:paddingLeft="@dimen/margin_gap_16"
            android:scaleType="centerCrop"
            android:src="@drawable/profile_photo"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_toRightOf="@+id/image_res"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/settingsgroup_txtview"
                android:layout_width="match_parent"
                android:layout_height="fill_parent"
                android:ellipsize="end"
                android:gravity="center_vertical"
                android:paddingLeft="@dimen/margin_gap_16"
                android:singleLine="true"
                android:text="@string/app_name"
                android:textAppearance="@style/TextAppearance.NormalFontRegular"
                android:textColor="@color/gray_text"
                android:textSize="@dimen/textsize_14"
                android:visibility="gone" />

            <EditText
                android:id="@+id/settingsgroup_edittxt"
                android:layout_width="match_parent"
                android:layout_height="fill_parent"
                android:ellipsize="end"
                android:gravity="center_vertical"
                android:imeOptions="actionDone"
                android:paddingLeft="@dimen/margin_gap_16"
                android:singleLine="true"
                android:text="Name"
                android:textAppearance="@style/TextAppearance.NormalFontRegular"
                android:textColor="@color/gray_text"
                android:textSize="@dimen/textsize_14"
                android:visibility="gone" />
        </LinearLayout>
    </RelativeLayout>

</LinearLayout>
