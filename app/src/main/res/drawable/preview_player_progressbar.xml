<?xml version="1.0" encoding="UTF-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />

            <gradient
                android:angle="270"
                android:centerColor="@color/preview_player_default"
                android:centerY="0.50"
                android:endColor="@color/preview_player_default"
                android:startColor="@color/preview_player_default" />
        </shape>
    </item>
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="5dip" />

                <gradient
                    android:angle="90"
                    android:endColor="@color/progressbar_color"
                    android:startColor="@color/progressbar_color" />
            </shape>
        </clip>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="5dip" />

                <gradient
                    android:angle="90"
                    android:endColor="@color/progressbar_color"
                    android:startColor="@color/progressbar_color" />
            </shape>
        </clip>
    </item>

</layer-list>